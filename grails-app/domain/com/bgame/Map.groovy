package com.bgame

class Map {
    int xaxis
    int yaxis
    String typ = "Feld"

    static constraints = {
    }

    
        String toString(){
            return "${typ}"
        }
        //allfields.sort(id)
        //allfields
    }
